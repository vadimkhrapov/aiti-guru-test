import { Typography } from 'antd'

const { Title } = Typography

function App() {
  return (
    <div style={{ padding: 24 }}>
      <Title level={1}>Заголовок</Title>
    </div>
  )
}

export default App
